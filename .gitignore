# dependencias
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
.turbo/
*.tsbuildinfo

# Prisma — cliente generado (y bases SQLite residuales de checkouts previos)
packages/db/src/generated/
packages/db/prisma/*.db
packages/db/prisma/*.db-journal
*.db
*.db-journal

# entorno (las llaves de Carlos NO se versionan)
.env
.env.local
.env.*.local
# excepción: el default local NO-secreto que la CLI de Prisma necesita (cwd packages/db)
!packages/db/.env

# logs / sistema
*.log
.DS_Store
coverage/

# herramientas locales del entorno (no son del proyecto)
.claude/
.playwright-mcp/
tavily-home.png
.vercel
